using Application.Interfaces;

namespace {{ namespace_ }};

public class {{ service_name }} : I{{ service_name }}
{
    public {{ service_name }}()
    {
        // TODO: inject dependencies
    }

{{ for op in operations }}
    public async Task<{{ op.responseType }}> {{ op.methodName }}Async({{ op.parameters }})
    {
        // TODO: implement
        await Task.Delay(10);
        return default!;
    }
{{ end }}
}